@model IEnumerable<EstradaDental.ViewModels.VMUserRoleName>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
    //int fotito = Model.archivos.Single(a => a.tipo == "Perfil").archivoID;
}



<h2>inicio de usuarios registrados</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombreCompleto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.rolID)
        </th>
        <th></th>
    </tr>



    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombreCompleto)
            </td>
            <%C# 
            <td>
                @Html.DisplayFor(modelItem => item.email)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.rolID)
            </td>
            <td>
                @*@if (fotito > 0) { 
                <img src="/Archivos/download/@fotito" />
                }
                else
                {
                    <img src="~/image/anonimo.jpg" />
                }*@

            </td>
            <td>

                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |

                @Html.ActionLink("Details", "Details", new { id = item.Id }) |

                @Html.ActionLink("Delete", "Delete", new { id = item.Id })

            </td>

        </tr>

    }



</table>
